#!/bin/sh
echo "Deprecated, now GitHub actions are responsible for it"
# VERSION=`cat ../fotokilof/version.py | grep version | awk '{print $3}' | tr -d '"'`
#echo "FotoKilof-$VERSION"
#python3 -m twine upload ../dist/FotoKilof-$VERSION*

#EOF

